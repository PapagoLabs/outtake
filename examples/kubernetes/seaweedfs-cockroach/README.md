# Suggested manifests: SeaweedFS + CockroachDB

Single-node insecure Cockroach for the example. Set the NFS server/path to Plex media (clips stay on S3).

```
kubectl apply -k examples/kubernetes/seaweedfs-cockroach
```
