Outtake {{ .Chart.Version }}

Local FS + SQLite is the default (same as docker-compose.yml).
Clips/metadata stay on storage-path or S3, never on the Plex media NFS.

Media source: media.nfs.server + media.nfs.path, or media.existingClaim.

Storage backend: {{ include "outtake.storageBackend" . }}
Database backend: {{ include "outtake.databaseBackend" . }}

This chart lives at examples/kubernetes/helm/outtake (no PapagoLabs Helm repo).
Distributed overlay: -f examples/kubernetes/helm/outtake/examples/values-distributed.yaml
Suggested manifests: examples/kubernetes/seaweedfs-cnpg/ and examples/kubernetes/seaweedfs-cockroach/.
Run `helm dependency update examples/kubernetes/helm/outtake` before any install.
